### Version 1

AE + Simplified VideoMark

Pipeline overview:

	Encoding:
		Every Nth frame is processed through a VAE Encoder
		Then a watermark is embedded into the latent layer
		Watermarked latent is then processed through a VAE Decoder
	
	Attack:
		IN PROGRESS

	Decoding:
		IN PROGRESS
